#include <vector>

void MergeSort(std::vector<int> &buf, size_t left, size_t right); //сортировка

void quickSort(std::vector<int> &value, long long left, long long right);

void insertion_sort(std::vector<int> &x);

